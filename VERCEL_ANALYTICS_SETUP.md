# 📊 Vercel Analytics — Настройка

## ✅ Что уже сделано

1. ✅ Установлен пакет `@vercel/analytics`
2. ✅ Добавлен `<Analytics />` компонент в `app/[locale]/layout.tsx`
3. ✅ Analytics начнёт работать автоматически после деплоя на Vercel

## Шаг 1: Включить Analytics в Vercel Dashboard

1. Зайди на https://vercel.com/dashboard
2. Открой свой проект
3. Перейди в раздел **Analytics** (в меню слева)
4. Нажми **Enable Analytics**
5. Готово! Analytics включена ✅

## Что будет отслеживаться

- 📈 **Уникальные посетители** (unique visitors)
- 📄 **Просмотры страниц** (page views)
- 🌍 **Страны и города** посетителей
- 📱 **Устройства** (desktop, mobile, tablet)
- 🌐 **Браузеры**
- ⏱️ **Время загрузки** страниц
- 🔗 **Источники трафика** (откуда пришли посетители)

## Преимущества Vercel Analytics

✅ **Бесплатно** для небольших проектов (до 2,500 посетителей/месяц)
✅ **Privacy-friendly** — не требует cookie баннера
✅ **Быстро** — не замедляет сайт
✅ **Интегрировано** с Vercel — работает "из коробки"
✅ **Server-side** — более точная статистика, чем Google Analytics

## Как посмотреть статистику

1. Зайди на https://vercel.com/dashboard
2. Открой свой проект
3. Перейди в **Analytics**
4. Там увидишь:
   - График посещений
   - Топ страниц
   - Страны посетителей
   - Устройства и браузеры

## 📊 Как показать статистику посетителям (опционально)

Если хочешь показывать публичную статистику посетителям сайта, можно использовать **Vercel API**:

### Вариант 1: Через Vercel API (сложнее, но гибче)

1. Создай **API Token** в Vercel:
   - Settings → Tokens → Create Token
   
2. Добавь токен в `.env.local`:
   ```bash
   VERCEL_API_TOKEN=your_token_here
   VERCEL_PROJECT_ID=your_project_id
   ```

3. Создай серверный компонент для получения статистики:
   ```tsx
   // app/components/AnalyticsStats.tsx
   async function getAnalytics() {
     const res = await fetch(
       `https://api.vercel.com/v1/analytics/${process.env.VERCEL_PROJECT_ID}`,
       {
         headers: {
           Authorization: `Bearer ${process.env.VERCEL_API_TOKEN}`,
         },
       }
     );
     return res.json();
   }

   export async function AnalyticsStats() {
     const data = await getAnalytics();
     
     return (
       <div className="text-center text-sm text-muted-foreground">
         📊 Сайт посещают более <strong>{data.visitors}</strong> уникальных гостей в месяц
       </div>
     );
   }
   ```

4. Добавь компонент в Footer:
   ```tsx
   import { AnalyticsStats } from './AnalyticsStats';
   
   export function Footer() {
     return (
       <footer>
         <AnalyticsStats />
         <p>© 2026 Dima Fomin</p>
       </footer>
     );
   }
   ```

### Вариант 2: Просто написать текст (проще)

Можешь просто добавить текст в Footer без динамической статистики:

```tsx
// components/Footer.tsx
<div className="text-center text-sm text-muted-foreground mb-4">
  📊 Сайт посещают повара и рестораторы из разных стран
</div>
```

Это создаёт **доверие** без технических сложностей.

## 🚀 Что делать дальше

1. **Deploy на Vercel** — Analytics заработает автоматически
2. **Включи Analytics** в Vercel Dashboard
3. **Подожди несколько дней** для накопления статистики
4. **Проверяй дашборд** и анализируй, какие статьи популярнее

## ⚠️ Ограничения бесплатного плана

- До **2,500 уникальных посетителей** в месяц (бесплатно)
- Если больше — нужен **Pro план** ($20/месяц)
- Для личного блога и небольших проектов — более чем достаточно

## 📌 Ссылки

- Документация: https://vercel.com/docs/analytics
- Pricing: https://vercel.com/docs/analytics/pricing
- API Reference: https://vercel.com/docs/rest-api/endpoints/analytics

---

## Готово! 🎉

Analytics уже подключена и начнёт собирать статистику после деплоя на Vercel.
